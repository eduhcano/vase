<h1>Listing users</h1>

<table>
	<ol>
	<% @users.each do |user| %>
    <li><%= link_to image_tag(user.profile.avatar.url(:thumb)), profile_path(user) %></li>
	<% end %>
	</ol>
</table>

<br />

<%= link_to 'New user', new_account_path %>